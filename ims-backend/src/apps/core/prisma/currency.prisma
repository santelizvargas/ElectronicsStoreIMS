model Currency {
    id           Int       @id @default(autoincrement())
    name         String    @unique
    iso2         String    @unique
    symbol       String
    country      String
    exchangeRate Float
    createdAt    DateTime
    udpatedAt    DateTime
    deletedAt    DateTime?
    invoice      Invoice?
}
