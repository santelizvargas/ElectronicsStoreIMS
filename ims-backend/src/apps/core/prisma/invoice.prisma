model Invoice {
    id                     Int       @id @default(autoincrement())
    customerName           String
    customerIdentification String
    totalAmount            Float
    products               Product[]
    createdAt              DateTime
    details                Detail[]
}
